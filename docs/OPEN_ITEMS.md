# פריטים פתוחים

פריטים שחסמים או מסייגים את מה שנבנה. כל שורה כאן מייצגת החלטה או נכס שצריך
להגיע מהלקוח לפני עלייה לאוויר.

## חוסם עלייה לאוויר

| # | נושא | מצב נוכחי באתר | מה נדרש |
| --- | --- | --- | --- |
| 1 | **קובץ APK** | `links.apk` מצביע על תיקיית Google Drive שהגיעה עם העיצוב המאושר | כתובת רשמית וקבועה לקובץ, וכן גרסה, תאריך שחרור, משקל קובץ ו‑SHA256. עד אז השדות ב‑`apkRelease` הם `null` ולכן פשוט לא מוצגים. אם `links.apk` יתרוקן — כפתור ה‑APK הופך אוטומטית ל״בקרוב״ ולא מקשר לשום מקום. |
| 2 | **עטיפות אלבומים** | 10 עטיפות ב‑`public/covers/` חתוכות מצילומי המסך של האפליקציה (`scripts/make-covers.mjs`) — למוקאפ בלבד | עטיפות מורשות לשימוש, או אישור מפורש לשימוש בעטיפות הקיימות באתר שיווקי |
| 3 | **תקנון, מדיניות פרטיות, זכויות יוצרים** | שלושת העמודים נכתבו במלואם ומתארים את מה שהמערכת באמת עושה (השדות ב‑`db/schema.ts`, הגיבוב ב‑`spam.ts`, ההעברה ב‑`webhook.ts`, ה‑`sessionStorage` ב‑`analytics.ts`). ה‑`noindex` הוסר | **בדיקת עורך דין לפני הסתמכות.** הנוסח נכתב על ידי מפתח, לא על ידי עורך דין, והוא נקודת פתיחה טובה — לא אישור משפטי |
| 3ב | **פרטי הישות המשפטית** | `legalEntity` ב‑`site.ts` מחזיק `companyId`, `address` ו‑`phone` כ‑`null`, ולכן הם פשוט לא מוצגים | ח״פ, כתובת רשומה וטלפון. הודעת פרטיות אמורה לזהות את מי שמחזיק במידע — כל עוד השדות ריקים העמוד מזהה רק בשם החברה ובדוא״ל |
| 3ג | **תקופת שמירת מידע** | `legalEntity.dataRetentionMonths` מוגדר ל‑24 חודשים, והמספר מוצג למשתמש במדיניות | אישור הלקוח למספר. זו התחייבות כלפי הפונה, ושינוי שלה הוא שינוי בנוסח המדיניות |
| 4 | **ספק אנליטיקס** | `src/lib/analytics.ts` מוכן וקורא ל‑`dataLayer`/`gtag` אם קיימים; בפיתוח האירועים נכתבים לקונסול. שום סקריפט צד־שלישי לא נטען | החלטה על ספק (GA4 / GTM / Vercel Analytics) ומזהה מדידה |
| 5 | **דומיין** | `site.url` מוגדר ל‑`https://jusic.co` | גישת DNS להפניה ל‑Vercel |

## דורש אישור ניסוח

| # | נושא | הערה |
| --- | --- | --- |
| 6 | ״נקייה ב‑100%״ ו״פלטפורמת התוכן היהודית המובילה״ | מופיעות בסקשן ״למה JUSIC״ בדיוק כפי שאושרו בעיצוב. לא הורחבו. |
| 7 | ״מודל התגמול דואג ליוצרים מהשקל הראשון״ | מופיעה בסקשן האמנים ובכרטיס ״כבוד ליוצרים״ |
| 8 | מחיר פרימיום 19.90 ₪ | מופיע בתשובת ה‑FAQ בלבד |
| 9 | הורדה אופליין ובקרת הורים | מוצגים כפיצ׳רים **עתידיים** בלבד, בתוך תשובת ה‑FAQ. אין להציגם כזמינים. |

## החלטות שנרשמות לתיעוד

| # | נושא | ההחלטה |
| --- | --- | --- |
| 10 | **פונט** | הבריף הציע Rubik/Heebo; מערכת העיצוב מגדירה **Ploni** (Fontef) והקבצים סופקו. נבחר Ploni. יש לוודא רישיון Web לשימוש בפרודקשן. |
| 11 | **אייקונים** | מערכת העיצוב טוענת Material Symbols Rounded ומסמנת זאת כתחליף בלבד. הבריף מגדיר `lucide-react`. נבחר lucide, עם מיפוי שמות ב‑`src/components/ui/Icon.tsx`. סמל Android הוא SVG מוטבע (אין לו גליף ב‑lucide). |
| 12 | **תמונות אווירה** | `public/atmos/` נוצרו ב‑Higgsfield לבקשת הלקוח, בניגוד לכלל ״אין רקעים פוטוגרפיים״ שבמערכת העיצוב. הן מופשטות בלבד, תמיד מתחת ל‑scrim, תמיד `alt=""`. |
| 13 | **Sanity** | לא נבנה בשלב זה בהחלטת הלקוח. כל התוכן יושב ב‑`src/content/site.ts` במבנה שנועד להתחלף במקור נתונים בלי שינוי ברכיבים. |
| 18 | **Next 16** | הפרויקט שודרג מ‑15.5 ל‑16.3 כי כל גרסאות `@neondatabase/auth` דורשות `next >= 16`. עלות השדרוג ב‑JS ההתחלתי היא כ‑30KB gz — ראו ARCHITECTURE §תקציבים. `middleware.ts` נקרא עכשיו `proxy.ts`, ו‑`agentRules: false` ב‑`next.config.ts` מונע מ‑Next להוסיף בלוק משלו ל‑CLAUDE.md בכל `next dev`. |
| 19 | **Neon Auth ב‑beta** | `@neondatabase/auth` מפורסם רק בתגי beta (`0.4.2-beta`), ולכן הגרסה **מוצמדת בדיוק** ב‑`package.json`. הוא מוגבל ל‑`/admin` ואינו נטען באף עמוד שיווקי. |
| 20 | **הרשאות דשבורד** | Neon Auth מאפשר לכל אחד להירשם. הגישה בפועל נקבעת ב‑`ADMIN_EMAILS`; רשימה ריקה נועלת את כולם ולא פותחת לכולם. |

## הצעות שלא מומשו

| # | נושא | הערה |
| --- | --- | --- |
| 14 | טופס יצירת קשר | ~~לא נבנה~~ **נבנה.** ארבעת נושאי הפוטר ו״דיווח על באג״ ב‑`/download` פותחים מודל, ולאמנים יש עמוד `/artists` עם טופס מלא. הפניות נשמרות ב‑Neon ונשלחות ל‑Make. הספאם מטופל ב‑honeypot, חותמת זמן חתומה ומכסה לפי IP. הפרטיות עדיין חסומה — ראו 3א. |
| 15 | גרסה אנגלית | לא נבנתה תשתית i18n. אם תידרש בעתיד, עדיף להחליט לפני שהתוכן גדל. |
| 16 | תפריט מובייל | הניווט ב‑`SiteHeader` הוא `hidden lg:flex` ואין כפתור המבורגר, כך שמתחת ל‑1024px אין ניווט כלל. בגלל זה `/artists` **לא** נוסף ל‑`nav` — הוא היה נעלם במובייל. הכניסות אליו הן סקשן היוצרים והפוטר. משימה נפרדת. |
| 17 | Turnstile / captcha | לא נבנה. שלוש שכבות ההגנה הנוכחיות אינן דורשות דבר מהמשתמש. אם יתחיל להגיע ספאם אמיתי, זו התוספת הבאה. |
